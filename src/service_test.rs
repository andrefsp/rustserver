use super::persistance::DBPersistence;

/*

use mockall::mock;

mock! {
    DB {}
    
    impl DBPersistence for DB {
        


    }
}
*/

#[tokio::test]
async fn test_service_fails() {
    assert!(false);
}
